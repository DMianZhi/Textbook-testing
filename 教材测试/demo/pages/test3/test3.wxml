<!-- success.wxml -->
<!-- 签到结果页 -->
<view class="page">
  <view class="title">{{result.title}}</view>
  <view class="info">
  <view class="meeting">{{result.meeting}}</view>
    <view class="name">{{result.name}}</view>
    <view class="time">{{result.time}}</view>
  </view>
</view>